//Question one
// Step 1: Create a variable with your best meal
//const bestMeal = "jollof rice and grilled chicken";

// Step 2: Combine it with your name
//const message = `Goodness loves ${bestMeal}`;

// Step 3: Print the final message
//console.log(message);

//Question two
// Step 1: Create two boolean variables
//const isPublicHoliday = true; // let's assume today is a public holiday
//const isWeekDay = true;       // and it's also a weekday

// Step 2: Use && and || to check conditions
//const isHolidayWeekday = isPublicHoliday && isWeekDay; // true if it's both a weekday and a public holiday
//const isHolidayOrWeekday = isPublicHoliday || isWeekDay; // true if it's either a public holiday or a weekday

// Step 3: Print the results
//console.log("Is it a weekday and a public holiday?", isHolidayWeekday); // true
//console.log("Is it a weekday or a public holiday?", isHolidayOrWeekday); // true

//Question three
/*
This script defines and calls a function named `add_three_num`.
The `add_three_num` function takes three numbers as arguments and returns their sum.
The result is then printed to the console.
*/

// Step 1: Define a function named `add_three_num` that takes three parameters
//function add_three_num(num1, num2, num3) {
    // Step 2: Add the three numbers and return the result
    //return num1 + num2 + num3;
//}

// Step 3: Call the function with example arguments and store the result
//let result = add_three_num(5, 10, 15);

// Step 4: Print the result to the console
//console.log("The sum of the three numbers is:", result);

//Question four
// Step 1: Create a variable named `today_day` and assign it a value
/* let today_day = 23;

// Step 2: Use a comparison operator to check if `today_day` is greater than 30
let isGreaterThanThirty = today_day > 30;

// Step 3: Print the result of the comparison
console.log("Is today_day greater than 30?", isGreaterThanThirty); */

//Question five
// Step 1: Create a variable `nick_name` and assign it your nickname
/*let nick_name = "Goodie";

// Step 2: Create a variable `count` and assign it a number
let count = 3;

// Step 3: Print both variables using console.log()
console.log("Nickname:", nick_name);
console.log("Count:", count); */

// Question six
// Step 1: Declare a function named `greet` that takes `name` and `timeOfDay` as parameters
/*function greet(name, timeOfDay) {
    // Step 2: Print a greeting message using the arguments
    console.log(`Good ${timeOfDay}, ${name}`);
}

// Step 3: Call the function with any name and time of day
greet("Goodness", "morning"); */

// Question seven
// Step 1: Create a variable without assigning any value
/*let myVariable;

// Step 2: Print the variable to see the output (should be undefined)
console.log("Initial value of myVariable:", myVariable);

// Step 3: Assign null to the same variable
myVariable = null;

// Step 4: Print the variable again to see the output
console.log("Value of myVariable after assigning null:", myVariable); */

//Question eight
// Step 1: Create two variables, x and y, and assign them values
/*let x = 12;
let y = 5;

// Step 2: Perform arithmetic operations
let sum = x + y;             // Addition
let difference = x - y;      // Subtraction
let product = x * y;         // Multiplication
let quotient = x / y;        // Division
let remainder = x % y;       // Modulo

// Step 3: Print the results of each operation
console.log("Sum:", sum);
console.log("Difference:", difference);
console.log("Product:", product);
console.log("Quotient:", quotient);
console.log("Remainder:", remainder); */

// Question nine
// Step 1: Create a variable `count` and assign it an initial value of 0
let count = 0;

// Step 2: Add 10 to the `count` variable
count += 10;

// Step 3: Multiply `count` by 20
count *= 20;

// Step 4: Print the final value of `count`
console.log("Final value of count:", count);
